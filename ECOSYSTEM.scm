;; SPDX-License-Identifier: PMPL-1.0-or-later
;; ECOSYSTEM.scm - Project relationship mapping

(ecosystem
  (version "1.0")
  (name "file-soup")
  (type "project")
  (purpose "FSLint provides contextual metadata about files through tiny, composable plugins")

  (position-in-ecosystem
    (role "component")
    (layer "application")
    (description "FSLint provides contextual metadata about files through tiny, composable plugins"))

  (related-projects . ())

  (what-this-is
    "FSLint provides contextual metadata about files through tiny, composable plugins")

  (what-this-is-not . ()))
